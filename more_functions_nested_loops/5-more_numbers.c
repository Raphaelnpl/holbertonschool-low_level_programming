#include "main.h"
/**
 * void more_numbers - check the code
 *
 * Return: Always 0.
 */
void more_numbers
{
	int x, y;

	for (x = 0; x <= 9; x++)
	{
		for (y = 0; y<= , y++)
		{
			if (y >= 10)
			{
				_putchar(y / 10 + '0');
			}
			_putchar(y % 10 + '0');
		}
		_putchar('\n');
	}
}	
